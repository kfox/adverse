## What this changes

<!-- What it does and why. If it closes an issue: "Closes #123". -->

## How it was verified

<!--
`npm test && npm run lint` is the gate CI runs. Say what else you did —
especially if a change affects the convergence loop, where the interesting
failures are non-terminating rather than red.
-->

- [ ] `npm test` is green
- [ ] `npm run lint` is clean
- [ ] Tests added or updated, and the new test was watched to FAIL against the
      unfixed code (a test that passes both ways pins nothing)

## Documentation

- [ ] `skills/adverse-review/SKILL.md` updated for a change to the loop, a
      phase, or an exit code
- [ ] `README.md` updated for a change a user would notice
- [ ] Not applicable

## Anything reviewers should know

<!-- Trade-offs, rejected approaches, follow-ups deliberately left out. -->
